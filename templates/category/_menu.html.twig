{# La variable categoryRepository est une variable globale déclarée dans config/packages/twig.yaml #}
{% for c in categoryRepository.findAll() %}

	<li class="nav-item">
		<a class="nav-link" href="{{ path('product_category', {'slug': c.slug}) }}">{{c.name}}</a>
	</li>

{% endfor %}
